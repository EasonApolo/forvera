// token过期时间
// JWT(JsonWebToken) options docs: https://github.com/auth0/node-jsonwebtoken
export const jwtExpiresDate = '30d'

// twit每页条数
export const twitPerPage = 10

// CORS 域名列表
export const corsDomains = ['http://forvera.me', 'http://localhost:10000']

export const anonymousNameList = ['']